version: '2.4'

services:
  app:
    tty: true
    build: 
      context: .
    shm_size: '30gb'
    volumes:
      - .:/app
      - ./store:/store
    environment:
      KAGGLE_USERNAME: ${KAGGLE_USERNAME:?}
      KAGGLE_KEY: ${KAGGLE_KEY:?}
